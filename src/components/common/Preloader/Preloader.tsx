import React from 'react';
import preholder from '../../../icon/Preloader.gif'

const Preloader: React.FC = () => {
    return <img src={preholder} style={{width: '200px'}} />
}

export default Preloader;